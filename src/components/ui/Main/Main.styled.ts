import {styled} from "styled-components";

export const MainWrapper = styled.div`
    width: 100vw;
    padding: 20px 15%;
    
    display: grid;
    grid-template: auto / 20% 80%;
    grid-gap: 20px;
`;